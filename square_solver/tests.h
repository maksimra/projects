#ifndef TESTS_H
#define TESTS_H

#include <stdio.h>
#include <stdbool.h> //добавить документацию к функциям, скачать утилиту и запустить, doxygen
#include <math.h>
#include "colors.h"

void tests      (void);

void print_root (double x1, double x2, int nRoots);

#endif //TESTS_H
